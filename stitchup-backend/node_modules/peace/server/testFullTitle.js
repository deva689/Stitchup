(function() {
    var self = this;
    module.exports = function(test) {
        var self = this;
        var testPath;
        testPath = [];
        while (test.parent !== void 0) {
            testPath.unshift(test.title);
            test = test.parent;
        }
        return testPath.join(".");
    };
}).call(this);